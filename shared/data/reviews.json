fetch('../shared/data/reviews.json')
  .then(res => res.json())
  .then(data => {
    const container = document.querySelector('.carousel');
    container.innerHTML = data.map(r => `<div class="carousel-slide">${r.text} — ${r.author}</div>`).join('');
    showSlide(0);
  });
